{
  "name": "@thuf/js-monorepo",
  "version": "0.1.0",
  "scripts": {
    "build": "pnpm install && turbo run build",
    "test": "pnpm install && turbo run test"
  },
  "devDependencies": {
    "turbo": "1.8.1",
    "typescript": "4.9.5",
    "esbuild": "0.15.18",
    "ts-jest": "29.0.5",
    "jest": "29.4.0",
    "@types/jest": "29.4.0",
    "@types/node": "18.14.0"
  }
}
